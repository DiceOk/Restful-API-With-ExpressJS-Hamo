<h1 class="text-center mb-3">{{title}}</h1>

<ul class="list-group-item" method="GET">
  {{#each members}}
  <li class="list-group-item">ID {{this.id}}: {{this.name}}: {{this.email}}</li>
  {{/each}}
</ul>

<br>
<a href="http://localhost:5000/" class="btn btn-primary">BACK TO FORM</a>